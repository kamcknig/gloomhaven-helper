import { BonusListPipe } from './bonus-list.pipe';

describe('BonusListPipe', () => {
  it('create an instance', () => {
    const pipe = new BonusListPipe();
    expect(pipe).toBeTruthy();
  });
});
