import { MonsterStatePipePipe } from './monster-stat.pipe';

describe('MonsterStatePipe', () => {
  it('create an instance', () => {
    const pipe = new MonsterStatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
