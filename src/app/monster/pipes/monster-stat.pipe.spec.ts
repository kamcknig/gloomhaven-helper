import {MonsterStatPipe} from "./monster-stat.pipe";

describe('MonsterStatePipe', () => {
  it('create an instance', () => {
    const pipe = new MonsterStatPipe();
    expect(pipe).toBeTruthy();
  });
});
