import { AttackEffectListPipe } from './attack-effect-list.pipe';

describe('AttackEffectListPipe', () => {
  it('create an instance', () => {
    const pipe = new AttackEffectListPipe();
    expect(pipe).toBeTruthy();
  });
});
