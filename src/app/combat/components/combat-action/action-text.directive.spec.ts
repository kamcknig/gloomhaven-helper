import {ActionTextDirective} from './action-text.directive';

describe('ActionTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionTextDirective();
    expect(directive).toBeTruthy();
  });
});
